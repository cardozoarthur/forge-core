# Forge Core v0.4.131 Self-Evolution Report

Run id: `run_35ef1be524e640b98b117d39b2d37998`  
Workflow id: `wf_19f0b1b0a91a4e8698a234932d9fa9e3`  
Prompt packet: `forge.self_evolution.prompt.v2`  
Cycle: 7  
Status: validated 0.5 milestone-boundary consistency update

## Summary

This cycle keeps Forge 0.5 readiness evidence consistent across the runtime,
public documentation and agent skill surfaces.

Before this patch, the runtime manifest and tests reported
`export_demo_baseline` as `validated` and the promotion decision as `promote`,
but `docs/forge-0.5-milestone.md` still showed the same capability as
`groundwork`. The packaged Forge skill also did not mention the
`forge milestone export-demo` CLI command or `forge.milestone.export_demo` MCP
tool, which made the agent-facing surface weaker than the runtime.

This is still a 0.4.x groundwork/release-boundary update. It does not change the
package line to 0.5; that remains an explicit human-controlled promotion step.

## Changed Files

- `Cargo.toml`
- `Cargo.lock`
- `src/skill.rs`
- `skills/forge-core/SKILL.md`
- `tests/forge_cli_contract.rs`
- `docs/forge-0.5-milestone.md`
- `README.md`
- `CHANGELOG.md`
- `docs/reports/forge-core-v0.4.131-report-2026-05-26.md`

## Runtime and Agent Surface

- Bumped the package version to `0.4.131`.
- Added a contract test that fails if the visible Forge 0.5 milestone boundary
  does not mark `Export/demo baseline` as `validated` after runtime validation.
- Added a contract test that fails if the packaged Forge skill omits
  `forge milestone export-demo` or MCP tool `forge.milestone.export_demo`.
- Updated the milestone document to match the runtime manifest and explain that
  the current promotion decision is generated by `forge milestone manifest`.
- Updated the packaged Codex/OpenCode skill content so agents can generate and
  inspect export/demo evidence through Forge-owned semantics.

## Validation

RED:

- `cargo test milestone_boundary_document_matches_validated_export_demo_runtime_state --test forge_cli_contract` failed because `docs/forge-0.5-milestone.md` still marked `Export/demo baseline` as `groundwork`.
- `cargo test packaged_skill_mentions_export_demo_agent_surface --test forge_cli_contract` failed because the packaged skill did not mention `forge milestone export-demo`.

GREEN:

- `cargo test milestone_boundary_document_matches_validated_export_demo_runtime_state --test forge_cli_contract`
- `cargo test packaged_skill_mentions_export_demo_agent_surface --test forge_cli_contract`
- `cargo fmt --check`
- `cargo clippy --all-targets --all-features -- -D warnings`
- `cargo test` (`27` unit tests, `201` CLI contract tests, doc-tests)
- `cargo build --release`
- `./target/release/forge --store /tmp/forge-core-v04131-plan.sqlite plan --goal "Create a delivery platform" --output json`
- `./target/release/forge --store /tmp/forge-core-v04131-skill.sqlite skill install --target codex --target opencode --output json --home /tmp/forge-skill-smoke-v04131`
- `./target/release/forge --store /tmp/forge-core-v04131-milestone.sqlite milestone manifest --version 0.5 --output json`
- `./target/release/forge --store /tmp/forge-core-v04131-export-demo.sqlite milestone export-demo --origin codex --output json`

The milestone manifest smoke returned `promotion_decision.decision = promote`
with an empty `blocked_by` list. The export-demo smoke generated a scheduled
`hackathon` workflow, screen artifact, document artifact, design token
collection and lineage chain in a temporary store.

## Install and Publish Notes

- `cargo install --path . --force` was attempted and blocked by this sandbox:
  `/home/arthur/.cargo/.crates.toml` is on a read-only filesystem.
- Local fallback install succeeded with
  `cargo install --path . --force --offline --root /home/arthur/projects/forge-core/.forge/local-install`.
- `/home/arthur/projects/forge-core/.forge/local-install/bin/forge --version`
  returned `forge 0.4.131`.
- `gh auth token` succeeded with output redirected away from chat.
- `git remote get-url origin` returned
  `https://github.com/cardozoarthur/forge-core.git`.
- `git add ... && git commit -m "Align Forge 0.5 milestone export demo surfaces"`
  was attempted after validation, but `.git/index.lock` could not be created
  because `.git` is read-only in this sandbox.
- `git push` was attempted and failed because `github.com` could not be
  resolved from this sandbox.

## Lean Overhead Ledger

- Prompt bytes: estimated 62,000.
- Estimated prompt tokens: estimated 15,500.
- Validation command count: 12 commands plus install/publish checks.
- New report artifacts: 1.
- Runtime/source files changed: 4.
- Documentation/skill/report files changed or added: 5.
- Metadata bytes added in this cycle: approximately 5,300.

## Safety

- No Docker, Kubernetes, Knative, Telegram or external user resources were
  mutated.
- All workflow/demo execution used temporary SQLite stores.
- The global install path was not modified because the sandbox blocked it.
- Forge remains the source of truth for milestone readiness; docs and skills now
  mirror the runtime surface instead of overriding it.

## Next Recommended Cycle

Run a human-controlled Forge 0.5 promotion decision cycle: generate the full
artifact bundle, verify whether remaining "gap before promotion" notes are
acceptable for the 0.5 baseline, update version-boundary docs deliberately, and
only then decide whether to bump the package line from 0.4.x to 0.5.
