# Forge 0.5 Milestone Boundary

Status vocabulary: `implemented`, `validated`, `groundwork`, `planned`, `blocked`.

Forge 0.5 is the first milestone allowed to claim the AI-first creative runtime, editable creative artifacts, live collaboration, whiteboards, design systems/tokens, componentization, AGUI-style interaction integration, web/CLI/MCP editing and human+AI creative workflows. The 0.4.x line may ship enabling infrastructure only.

| Capability | Status | Evidence | Gap before 0.5 promotion |
| --- | --- | --- | --- |
| Interactive Forge CLI baseline | validated | 0.4.97-0.4.109 validate `forge` TTY home, slash-command catalog, conversational routing, retention decisions and script-safe non-TTY behavior. 0.4.147 adds `/milestone`, `/manifest` and `/research` slash commands to the interactive REPL. | Full terminal TUI loop, autocomplete and richer inline mode remain before a 0.5 promotion claim. |
| Human decision/form nodes | validated | 0.4.98-0.4.109 validate choice prompts, form schemas, durable decisions, timeout state, pause/resume, list/status/inspect visibility and MCP create/list/answer/expire bridges. | Web UI, repeated-answer default promotion and richer TUI rendering remain planned. |
| Scheduler/loop/subflow foundation | validated | 0.4.92-0.4.112 validate cron nodes, loop state, due execution, missed-run reconciliation, native scale-to-zero decisions, daily Goal research smoke artifacts, schedule visibility and MCP aggregate schedule/loop summaries. | Production executor adapters for real research/page inspection remain planned. |
| Milestone governance/status surface | validated | 0.4.100 adds `forge milestone status --version 0.5 --output json` and MCP tool `forge.milestone.status` with promotion blockers. | Keep this surface updated as creative runtime capabilities move from planned to implemented or validated. |
| Creative artifact IR baseline | validated | 0.4.102 validates structured creative artifact IR for screens, whiteboards, documents, slide decks and component specs with serde round-trip, CLI attach/list/inspect coverage and workflow integration. | Declarative import/export, rendering adapters and full runtime editing flows remain for 0.5. |
| Design systems/tokens | validated | 0.4.125 validates raw token + semantic alias resolution, mode overrides, workflow token summaries, impact previews and targeted token patch-by-intent through CLI and MCP without rewriting creative artifacts. | Inheritance across token collections, rendered propagation previews and richer human edit preservation demos remain before 0.5 promotion. |
| Componentization and AI-first UI surfaces | validated | 0.4.125 validates token dependency impact detection across creative artifacts and records token edits as structured `PatchByIntent` diffs. | Rendered component previews, action registry generation and AI-driven component generation remain for 0.5. |
| Live collaboration | validated | 0.4.98-0.4.104 validate human decision audit and MCP human interaction bridges. 0.4.127 adds Forge-owned creative collaboration state with presence, cursors/selections, comments, patch streams, conflict records, rollback records, audit history, CLI event/status commands, MCP collaboration tools and screen/document contract tests. | Full browser live editing transport, multi-user conflict resolution UX and richer rollback visualization remain before a final 0.5 promotion claim. |
| Research artifact baseline | validated | 0.4.129 adds `docs/research/forge-0.5-creative-runtime-source-research.md`, `forge milestone research --version 0.5 --output json` and MCP tool `forge.milestone.research`. The source-grounded comparison covers Penpot, Stitch, v0, AG-UI, Impeccable, Figma MCP, Remotion, OBS and local Superpowers/Stitch/imagegen/Figma/Remotion skills, then converts findings into Forge validation gates, workflow templates and lean governance decisions. | Keep the research artifact current as external creative/runtime protocols and local skills drift; no 0.5 promotion claim should bypass the full milestone manifest. |
| Export/demo baseline | validated | 0.4.130 adds `forge milestone export-demo` and MCP tool `forge.milestone.export_demo`, generating a Forge-owned scheduled daily research workflow with screen/document creative artifacts, a design token collection and lineage evidence. Daily Goal smoke continues to produce Markdown/PDF artifacts and Telegram delivery records through Forge-owned workflow semantics. | Full rendered previews and richer browser-based editing demos remain for later 0.5 iterations, but the required export/demo baseline now has runtime evidence. |
| Replacement-grade Forge CLI | groundwork | 0.4.x validates the no-argument interactive home, slash commands, conversational routing, human decisions, async run handoff and observability surfaces. `forge milestone cli-demo` and MCP tool `forge.milestone.cli_demo` generate deterministic Forge-first demo evidence for coding, research/artifact and long-running async flows. 0.4.146 adds registry-level run health summaries so `forge list` and `forge inspect` expose running, stale and missing-heartbeat runs even when no heartbeat is currently fresh. 0.4.148 adds process-liveness-aware run activity so a recorded live executor PID keeps long-running handoffs active after heartbeat TTL expiry instead of forcing stale recovery. 0.4.150 adds `forge patch plan` and MCP tool `forge.patch.plan` as a plan-only file-editing contract with repo-relative permission gates, file snapshots, diff-review commands, validation commands and workflow artifact lineage without applying changes. 0.4.151 adds apply artifacts plus guarded revert proposals: `forge patch revert` records approval commands and rollback lineage without silently running `git checkout`. 0.4.153 adds interactive `/context` and `/handoff` commands so operators can inspect bounded context routes and approve executor handoff lease acquisition from inside `forge`. 0.4.154 exposes `forge.interactive.home`, `forge.interactive.slash_commands` and `forge.interactive.route` through MCP so agents can inspect and use the same interactive command/chat routing model without launching a TTY or taking over orchestration. `forge shells --executor <id>` and MCP `forge.shell.launch_plan` now return plan-only shell launch reports with readiness, selected entrypoint, harness status, preflight commands and concrete context/handoff/heartbeat commands when workflow/task/run are supplied, without starting child processes. `forge shells --record-session` and MCP `forge.shell.record_plan` write `forge.shell_session_receipt.v1` plus `shell_launch_planned` timeline events for auditable shell intent. `forge sessions` and MCP `forge.sessions` expose `forge.brain_sessions.v1` with provider summaries, shell session readiness and recent shell launch events; `forge sessions lifecycle`, MCP `forge.session.lifecycle` and `/sessions lifecycle` add audit-only opened/attached/closed session lifecycle events. `forge harness wrap-plan`, shims, `forge harness exec` and MCP `forge.harness.exec` now carry workflow/task/run lineage through env, launch commands, receipts and `forge.harness.exec_event.v1` global timeline events, including receipt metadata and headroom refs. Project `.forge/harness.json` now also supports `require_lineage_for_exec`, returning `harness_exec_blocked_by_project_policy` instead of launching real child execution when workflow/task/run lineage is missing. 0.4.177 adds `forge harness mode`, MCP `forge.harness.mode`, the interactive home `harness_mode_panel` and `/harness` slash command so humans and agents can audit the effective Forge-first policy, precedence and project config source before launching brain CLIs. Current patch lifecycle adds `forge patch review`, MCP `forge.patch.review` and `/patch review` with `forge.patch_review.v1` evidence for Git diff/status/check review before approval, `forge patch diff`, MCP `forge.patch.diff` and `/patch diff` with `forge.patch_diff.v1` evidence for read-only multi-file diff navigation, plus `forge patch restore`, MCP `forge.patch.restore` and `/patch restore` with `forge.patch_restore.v1` evidence for explicit, approved repo-local file restoration from a revert artifact. | Add richer file editing permission/editing UX, deeper provider/session lifecycle controls and end-to-end coding/research workflows before promoting this beyond groundwork. |
| Experimental multimodal runtime | groundwork | 0.4.140 adds disabled-by-default multimodal inventory, plan-only install manifests and runtime guards for camera, microphone, screen, input, peripherals, model and filesystem access. 0.4.142 adds `forge multimodal benchmark-template` and `forge multimodal demo-plan`, plus MCP tools `forge.multimodal.benchmark_template` and `forge.multimodal.demo_plan`, for plan-only benchmark/report templates and guarded local image/audio/Blender demo plans without installs or device access. The current line adds approved `.forge/multimodal.json` feature-flag configuration, `--project-root`/MCP project-root inspection, approval-gated `forge multimodal benchmark-result` and MCP `forge.multimodal.benchmark_result` fixture-only artifacts with explicit no-install, no-model-execution, no-device-access and no-network-access evidence. | Add real guarded benchmark/demo evidence after opt-in, proving no camera/microphone/screen/input/filesystem access occurs without guard approval. |

Promotion rule: a future `0.5` manifest must fail promotion if any required creative/runtime capability is only a goal, prompt item or partial infrastructure without working demo evidence. Current promotion decision is produced by `forge milestone manifest --version 0.5 --output json`; as of 0.4.177, the manifest reports `fail` because replacement-grade CLI and experimental multimodal runtime remain `groundwork`.

Use `forge milestone manifest --version 0.5 --output json` or MCP tool `forge.milestone.manifest` to generate the release-gate manifest with requirements, completed capabilities, missing capabilities, validation evidence, demos, known gaps and the current promotion decision.
