# Forge 0.5 Milestone Boundary

Status vocabulary: `implemented`, `validated`, `groundwork`, `planned`, `blocked`.

Forge 0.5 is the first milestone allowed to claim the AI-first creative runtime, editable creative artifacts, live collaboration, whiteboards, design systems/tokens, componentization, AGUI-style interaction integration, web/CLI/MCP editing and human+AI creative workflows. The 0.4.x line may ship enabling infrastructure only.

| Capability | Status | Evidence | Gap before 0.5 promotion |
| --- | --- | --- | --- |
| Interactive Forge CLI baseline | validated | 0.4.97-0.4.109 validate `forge` TTY home, slash-command catalog, conversational routing, retention decisions and script-safe non-TTY behavior. 0.4.147 adds `/milestone`, `/manifest` and `/research` slash commands to the interactive REPL. The current line adds read-only autocomplete evidence through `forge interactive autocomplete`, MCP `forge.interactive.autocomplete` and `dashboard.autocomplete_panel`. | Full terminal TUI loop and richer inline mode remain before a 0.5 promotion claim. |
| Human decision/form nodes | validated | 0.4.98-0.4.109 validate choice prompts, form schemas, durable decisions, timeout state, pause/resume, list/status/inspect visibility and MCP create/list/answer/expire bridges. | Web UI, repeated-answer default promotion and richer TUI rendering remain planned. |
| Scheduler/loop/subflow foundation | validated | 0.4.92-0.4.112 validate cron nodes, loop state, due execution, missed-run reconciliation, native scale-to-zero decisions, daily Goal research smoke artifacts, schedule visibility and MCP aggregate schedule/loop summaries. | Production executor adapters for real research/page inspection remain planned. |
| Milestone governance/status surface | validated | 0.4.100 adds `forge milestone status --version 0.5 --output json` and MCP tool `forge.milestone.status` with promotion blockers. The current line adds `forge milestone evidence-plan` and MCP `forge.milestone.evidence_plan` so operators can inspect project `.forge` manifests, real provider/runtime readiness, missing receipt kinds and exact collection/attach commands before claiming evidence. It also adds `forge milestone collect-evidence --kind <evidence-kind>`, MCP `forge.milestone.collect_evidence`, `forge milestone attach-evidence`, MCP `forge.milestone.attach_evidence` and `attached_evidence` in `forge milestone manifest`, allowing ready provider/runtime/demo sources to generate receipts, copy them into the Forge store and record them on the global event timeline without auto-promoting capabilities. `forge interactive release-gates`, MCP `forge.interactive.release_gates` and the home dashboard now surface the same attached evidence counts and per-capability receipts. | Keep this surface updated as creative runtime capabilities move from planned to implemented or validated. |
| Creative artifact IR baseline | validated | 0.4.102 validates structured creative artifact IR for screens, whiteboards, documents, slide decks and component specs with serde round-trip, CLI attach/list/inspect coverage and workflow integration. | Declarative import/export, rendering adapters and full runtime editing flows remain for 0.5. |
| Design systems/tokens | validated | 0.4.125 validates raw token + semantic alias resolution, mode overrides, workflow token summaries, impact previews and targeted token patch-by-intent through CLI and MCP without rewriting creative artifacts. | Inheritance across token collections, rendered propagation previews and richer human edit preservation demos remain before 0.5 promotion. |
| Componentization and AI-first UI surfaces | validated | 0.4.125 validates token dependency impact detection across creative artifacts and records token edits as structured `PatchByIntent` diffs. The current line adds an initial read-only action registry through `forge interactive action-registry`, MCP `forge.interactive.action_registry` and `dashboard.action_registry_panel`, deriving operation-ready actions from governed TUI Addon view contracts. | Rendered component previews, richer component-scoped action registries and AI-driven component generation remain for 0.5. |
| Live collaboration | validated | 0.4.98-0.4.104 validate human decision audit and MCP human interaction bridges. 0.4.127 adds Forge-owned creative collaboration state with presence, cursors/selections, comments, patch streams, conflict records, rollback records, audit history, CLI event/status commands, MCP collaboration tools and screen/document contract tests. | Full browser live editing transport, multi-user conflict resolution UX and richer rollback visualization remain before a final 0.5 promotion claim. |
| Research artifact baseline | validated | 0.4.129 adds `docs/research/forge-0.5-creative-runtime-source-research.md`, `forge milestone research --version 0.5 --output json` and MCP tool `forge.milestone.research`. The source-grounded comparison covers Penpot, Stitch, v0, AG-UI, Impeccable, Figma MCP, Remotion, OBS and local Superpowers/Stitch/imagegen/Figma/Remotion skills, then converts findings into Forge validation gates, workflow templates and lean governance decisions. | Keep the research artifact current as external creative/runtime protocols and local skills drift; no 0.5 promotion claim should bypass the full milestone manifest. |
| Export/demo baseline | validated | 0.4.130 adds `forge milestone export-demo` and MCP tool `forge.milestone.export_demo`, generating a Forge-owned scheduled daily research workflow with screen/document creative artifacts, a design token collection and lineage evidence. Daily Goal smoke continues to produce Markdown/PDF artifacts and Telegram delivery records through Forge-owned workflow semantics. | Full rendered previews and richer browser-based editing demos remain for later 0.5 iterations, but the required export/demo baseline now has runtime evidence. |
| Replacement-grade Forge CLI | groundwork | 0.4.x validates the no-argument interactive home, slash commands, conversational routing, human decisions, async run handoff and observability surfaces. `forge milestone cli-demo` and MCP tool `forge.milestone.cli_demo` generate deterministic Forge-first demo evidence for coding, harness/headroom/session lifecycle control, brain handoff rehearsal, research/artifact and long-running async flows, including `forge.milestone.patch_lifecycle_demo.v1` with plan/review/diff/apply/revert/restore artifact lineage in an isolated fixture repo, `forge.milestone.executor_project_demo.v1`, which proves a deterministic executor can mutate an isolated project only after governed harness bootstrap, lineage-required execution, event recording and stdout token-headroom retrieval, and `forge.milestone.brain_handoff_demo.v1`, which proves Forge-owned context, node-brain routing, task handoff, plan-only shell launch and audit-only session lifecycle for Codex without child CLI/model execution. 0.4.146 adds registry-level run health summaries so `forge list` and `forge inspect` expose running, stale and missing-heartbeat runs even when no heartbeat is currently fresh. 0.4.148 adds process-liveness-aware run activity so a recorded live executor PID keeps long-running handoffs active after heartbeat TTL expiry instead of forcing stale recovery. 0.4.150 adds `forge patch plan` and MCP tool `forge.patch.plan` as a plan-only file-editing contract with repo-relative permission gates, file snapshots, diff-review commands, validation commands and workflow artifact lineage without applying changes. 0.4.151 adds apply artifacts plus guarded revert proposals: `forge patch revert` records approval commands and rollback lineage without silently running `git checkout`. 0.4.153 adds interactive `/context` and `/handoff` commands so operators can inspect bounded context routes and approve executor handoff lease acquisition from inside `forge`. 0.4.154 exposes `forge.interactive.home`, `forge.interactive.slash_commands` and `forge.interactive.route` through MCP so agents can inspect and use the same interactive command/chat routing model without launching a TTY or taking over orchestration. `forge shells --executor <id>` and MCP `forge.shell.launch_plan` now return plan-only shell launch reports with readiness, selected entrypoint, harness status, preflight commands and concrete context/handoff/heartbeat commands when workflow/task/run are supplied, without starting child processes. `forge shells --record-session` and MCP `forge.shell.record_plan` write `forge.shell_session_receipt.v1` plus `shell_launch_planned` timeline events for auditable shell intent. `forge sessions` and MCP `forge.sessions` expose `forge.brain_sessions.v1` with provider summaries, shell session readiness and recent shell launch events; `forge sessions lifecycle`, MCP `forge.session.lifecycle` and `/sessions lifecycle` add audit-only opened/attached/closed session lifecycle events. The current line deepens that lifecycle with `previous_state`, `lifecycle_sequence`, `transition` policy, ordered transition rejection, `lifecycle_policy.allowed_next_states`, per-session `forge.brain_session_operation_plan.v1`, next lifecycle commands and provider/state/readiness filters in `forge sessions` plus MCP `forge.sessions`, and `forge sessions history`, MCP `forge.session.history` and `/sessions history` for per-session chronological audit history. `forge harness wrap-plan`, shims, `forge harness exec` and MCP `forge.harness.exec` now carry workflow/task/run lineage through env, launch commands, receipts and `forge.harness.exec_event.v1` global timeline events, including receipt metadata and headroom refs. Project `.forge/harness.json` now also supports `require_lineage_for_exec`, returning `harness_exec_blocked_by_project_policy` instead of launching real child execution when workflow/task/run lineage is missing. `forge harness doctor`, MCP `forge.harness.doctor`, `/harness doctor` and the interactive home `harness_doctor_panel` provide consolidated readiness audits, and `forge harness mode --project-root` and MCP `forge.harness.mode` `project_root` can audit another project's harness policy before launching a brain CLI. `forge harness wrap-plan --project-root` and MCP `forge.harness.wrap_plan` `project_root` make wrapper planning respect another project's Forge-first defaults before shell execution. `forge harness install-shims --project-root` and MCP `forge.harness.install_shims` `project_root` make shim installation use the same remote project defaults. `forge harness exec --project-root` and MCP `forge.harness.exec` `project_root` apply those defaults and lineage policy without changing the child `--cwd`. 0.4.177 adds `forge harness mode`, MCP `forge.harness.mode`, the interactive home `harness_mode_panel` and `/harness` slash command so humans and agents can audit the effective Forge-first policy, precedence and project config source before launching brain CLIs. Current patch lifecycle adds `forge patch review`, MCP `forge.patch.review` and `/patch review` with `forge.patch_review.v1` evidence for Git diff/status/check review before approval, `forge patch diff`, MCP `forge.patch.diff` and `/patch diff` with `forge.patch_diff.v1` evidence for read-only multi-file diff navigation, `forge interactive patch-workbench`, MCP `forge.interactive.patch_workbench` and `dashboard.patch_workbench_panel` with bounded inline `diff_preview`, multi-file `diff_review_queue`, `forge.interactive.patch_edit_intake.v1` required-input/form readiness and `forge.interactive.patch_operation_plan.v1` ordered lifecycle steps for safe file-editing UI, plus `forge patch restore`, MCP `forge.patch.restore` and `/patch restore` with `forge.patch_restore.v1` evidence for explicit, approved repo-local file restoration from a revert artifact. `forge interactive release-gates`, MCP `forge.interactive.release_gates` and `dashboard.release_gates_panel` now expose the milestone promotion decision, blocked capabilities, required evidence, current evidence and next commands as a read-only operator panel. | Add broader brain-driven real-project coding/research workflows and continue hardening terminal file editing UX before promoting this beyond groundwork. |
| Experimental multimodal runtime | groundwork | 0.4.140 adds disabled-by-default multimodal inventory, plan-only install manifests and runtime guards for camera, microphone, screen, input, peripherals, model and filesystem access. 0.4.142 adds `forge multimodal benchmark-template` and `forge multimodal demo-plan`, plus MCP tools `forge.multimodal.benchmark_template` and `forge.multimodal.demo_plan`, for plan-only benchmark/report templates and guarded local image/audio/Blender demo plans without installs or device access. The current line adds approved `.forge/multimodal.json` feature-flag configuration, `--project-root`/MCP project-root inspection, `forge multimodal readiness` and MCP `forge.multimodal.readiness` for runtime PATH/model-manifest inspection without child execution, approval-gated `forge multimodal benchmark-result` and MCP `forge.multimodal.benchmark_result` fixture-only artifacts with explicit no-install, no-model-execution, no-device-access and no-network-access evidence, approval-gated `forge multimodal runtime-benchmark` and MCP `forge.multimodal.runtime_benchmark` guarded deterministic local runtime execution after opt-in with model guard approval while installs, devices, filesystem and network remain blocked, plus `forge multimodal demo-receipt` and MCP `forge.multimodal.demo_receipt` guarded local fixture receipts after opt-in with model guard approval recorded and camera/microphone/screen/input/filesystem access blocked unless separately approved. Multimodal is now declared through `forge.addon.multimodal` with capability `multimodal_runtime`, permission `multimodal.runtime_benchmark`, view `multimodal.benchmark_center` and runtime contract `multimodal_runtime_benchmark.executor`; the Core command path remains a guarded compatibility executor, and Addon runtime dispatch can execute it through `forge.addons.run_dispatch` with policy/lineage evidence. | Add production model/runtime benchmark evidence after opt-in; current readiness, runtime benchmark and demo receipts prove guard behavior but still avoid installs, production model execution and device/filesystem access by default. |

Current replacement-grade CLI groundwork also includes `forge interactive harness`, MCP `forge.interactive.harness` and `dashboard.harness_panel`, which consolidate harness mode, doctor, shim status, wrapper plan, `headroom_plan`, first-class `session_lifecycle_plan` and token-headroom preview as a Core UI widget for TUI/web/agent clients without launching child CLIs. `forge harness headroom-plan` and MCP `forge.harness.headroom_plan` now expose the effective context budget, token-headroom source, wrapper env, `forge.harness.session_lifecycle_plan.v1`, compression pipeline, reserve strategy and next commands as a dedicated read-only contract before a brain CLI receives large payloads. `forge harness adoption-plan` and MCP `forge.harness.adoption_plan` now aggregate harness mode, headroom and doctor evidence into an ordered read-only adoption plan for project `.forge/harness.json`, Forge-first shims, executor sync and lineage-required execution, explicitly reporting that it neither mutates state nor launches child CLIs. `forge harness bootstrap` and MCP `forge.harness.bootstrap` add the governed apply path: dry-run is default, while `--apply --approved-by <operator>` writes the project harness policy and installs Forge-owned shims through the same non-destructive installer. It also includes `forge interactive sessions`, MCP `forge.interactive.sessions` and `dashboard.sessions_panel`, which consolidate provider/session readiness, lifecycle state, session `operation_plan` controls and next lifecycle/history commands without opening or attaching shells implicitly. `forge interactive patch-workbench`, MCP `forge.interactive.patch_workbench` and `dashboard.patch_workbench_panel` now expose an explicit `approval_flow` with review, human-approval and rollback gates, `edit_intake` with required inputs and form readiness, plus bounded inline `diff_preview` and multi-file `diff_review_queue` before any file-editing UI offers apply/restore actions. `forge interactive command-palette`, MCP `forge.interactive.command_palette` and `dashboard.command_palette_panel` expose grouped contextual actions for navigation, workflows, Addon CLI actions, permissions, harness, sessions and observability without mutating workflow state; enabled TUI Addon views can now declare palette metadata and command templates through `AddonViewAction`, and Addon actions preserve generic Addon/capability/permission/view/action lineage through `forge.interactive.addon_action_contract.v1`, `addon_view_id`, `addon_view_action_id`, `enabled`, `blocked_reason` and `operation_plan` using `forge.interactive.command_palette_action_plan.v1`. `forge interactive action-registry`, MCP `forge.interactive.action_registry`, interactive `/actions [query]` and `dashboard.action_registry_panel` expose a strict-filtered read-only action registry with per-group readiness counts and the same operation plans for TUI/web/agent clients that should not depend on command-palette layout. `forge interactive action-invocation`, MCP `forge.interactive.action_invocation` and interactive `/action <action-id>` resolve one selected action into a ready command or diagnostic-only operation plan with `not_executed=true`, so clients can separate selection/readiness from actual execution. `forge interactive autocomplete`, MCP `forge.interactive.autocomplete` and `dashboard.autocomplete_panel` expose read-only slash-command, command-palette and `/action <partial>` action-id suggestions for partial operator input, including mutation, approval, readiness and operation-plan flags, and command-palette plus action-id suggestions keep Addon lineage when they originate from Addon actions; blocked Addon actions stay diagnosable, have empty command-palette insert/equivalent commands and point to Addon view inspection as the diagnostic next step.

Promotion rule: a future `0.5` manifest must fail promotion if any required creative/runtime capability is only a goal, prompt item or partial infrastructure without working demo evidence. Current promotion decision is produced by `forge milestone manifest --version 0.5 --output json`; as of 0.4.177, the manifest reports `fail` because replacement-grade CLI and experimental multimodal runtime remain `groundwork`.

Use `forge milestone manifest --version 0.5 --output json` or MCP tool `forge.milestone.manifest` to generate the release-gate manifest with requirements, completed capabilities, missing capabilities, validation evidence, attached evidence, demos, known gaps and the current promotion decision. Use `forge milestone evidence-plan --version 0.5 --capability <capability-id> --project-root <project-root> --output json` or MCP `forge.milestone.evidence_plan` to inspect whether project manifests are ready for real provider/runtime evidence collection. Use `forge milestone collect-evidence --version 0.5 --capability <capability-id> --kind <evidence-kind> --project-root <project-root> --approved-by <operator> --output json` or MCP `forge.milestone.collect_evidence` to run a ready source, persist the receipt and attach it; omit `--kind` only for the capability default receipt. Use `forge milestone attach-evidence --version 0.5 --capability <capability-id> --kind <kind> --summary <summary> --artifact <path> --approved-by <operator> --output json` or MCP `forge.milestone.attach_evidence` to attach already-reviewed external runtime/provider receipts; attached evidence is audit state, not a promotion override.
