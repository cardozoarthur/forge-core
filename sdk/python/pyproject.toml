[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "forge-sdk"
version = "0.0.0"
description = "Forge Python SDK scaffold for async workflow orchestration."
requires-python = ">=3.10"

[tool.setuptools]
packages = ["forge_sdk"]

